root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main${GOEXE} ."
  bin = "./tmp/main${GOEXE}"
  full_bin = "./tmp/main${GOEXE} --server-only"
  include_ext = ["go"]
  exclude_dir = ["tmp"]
  delay = 500

[log]
  time = false

[misc]
  clean_on_exit = true
